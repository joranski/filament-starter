<div class="flex items-center justify-between">
    <img
        src="{{ asset('assets/logos/va-swm.png', config('app.https')); }}"
        alt="{{ config('app.name'); }}"
        loading="lazy"
        style="height: 2.5rem;"
    />
    <h1 class="ml-4 font-bold text-xl intalic">{{ config('app.name'); }}</h1>
</div>